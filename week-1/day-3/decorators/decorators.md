## Decorators

A decorator in Python is a way to modify or extend the behavior of a function (or class) without changing its actual code.

Think of it like this:

- You already have a function
- You want to add something before/after it runs
- Instead of editing the function itself, you wrap it with another function

### examples

- retry
- timer
- logger/trace
- ratelimiting
- caching
